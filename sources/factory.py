

class Source:
    keyname = self.keyname
    provider = self.provider
    table = self.table
    #def inspect()
    def run(level):
        assert level in ['gather', 0,1,2], "specify a correct level to process"
        if level
    def upload()
    def update()